// Adds `toHaveAttribute` and friends to `expect`, for the tests that read a
// rendered element's DOM the way a browser would rather than by re-deriving
// its props from the component tree.
import "@testing-library/jest-dom/vitest";
